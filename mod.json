{
	"geode": "5.3.0",
	"gd": {
		"mac": "2.2081"
	},
	"id": "zmx.sdl",
	"name": "SDL Mod",
	"version": "v1.0.1",
	"developer": "zmx",
	"description": "Full rewrite of the windowing layer on macOS",
	"early-load": true,
	"tags": ["interface", "bugfix", "enhancement", "universal", "performance", "offline"],
	"links": {
		"source": "https://github.com/qimiko/sdl-mod"
	},
	"settings": {
		"category-performance": {
			"type": "title",
			"name": "Performance"
		},
		"disable-vsync": {
			"type": "bool",
			"name": "Disable VSync",
			"description": "Allows for <cj>manual control</c> of frame timing.",
			"default": false
		},
		"uncap-framerate": {
			"type": "bool",
			"name": "Disable Framerate Cap",
			"description": "Removes the <cl>framerate cap</c>. This has a <co>significant</c> performance cost!",
			"default": false,
			"enable-if": "disable-vsync"
		},
		"framerate-limit": {
			"type": "int",
			"name": "Target Framerate",
			"min": 10,
			"default": 60,
			"enable-if": "disable-vsync && !uncap-framerate"
		},
		"exclusive-fullscreen": {
			"type": "bool",
			"name": "Exclusive Fullscreen",
			"description": "Recontexualizes the <cl>ingame fullscreen option</c> to use <cg>exclusive fullscreen</c>, allowing for it to <cp>draw underneath</c> display cutouts.\n\nExclusive fullscreen <cr>acts strangely</c> on macOS, including **desktop softlocks** and **odd crashes**. Geometry Dash also does not properly support vertical display cutouts, so it is <co>not recommended</c> to enable this unless you are _<cd>prepared</c>_.",
			"default": false
		},
		"disable-high-dpi": {
			"type": "bool",
			"name": "Disable High DPI Mode",
			"description": "Disables <cp>High DPI</c> mode on <cl>Retina displays</c>, meaning the game will run at a <cr>lower resolution</c> that is then upscaled to fit the display (typically <cy>2x scale</c>). Enabling may <cg>improve performance</c> on GPU heavy levels, as well as <cj>improve scaling</c> on some overlay mods.",
			"default": false,
			"requires-restart": true
		},
		"category-visual": {
			"type": "title",
			"name": "Visual"
		},
		"window-title": {
			"type": "string",
			"name": "Window Title",
			"description": "Geometry dsash",
			"default": "Geometry Dash*"
		},
		"p3-color-space": {
			"type": "bool",
			"name": "Revert Color Space",
			"description": "Resets the <cd>color space</c> used on <cs>HDR displays</c> to the <cl>vanilla</c> setting, **Display P3**. This color space appears <cg>more vivid</c>, but is <cr>inaccurate</c> to the real colors.\n\nRealistically, <co>neither</c> option is <cp>perfectly accurate</c> due to how these displays work.",
			"default": false,
			"requires-restart": true
		},
		"unlock-aspect-ratio": {
			"type": "bool",
			"name": "Unlock Aspect Ratio",
			"description": "Enables free resizing.",
			"default": false,
			"requires-restart": true
		},
		"use-ime": {
			"type": "bool",
			"name": "Enable IME",
			"description": "Uses SDL's <cl>IME capabilities</c> for text inputs, which makes it possible to enter specific <cc>special characters</c> into text fields. Some mods may not handle this correctly.",
			"default": true
		}
	}
}
